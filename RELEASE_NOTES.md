## DetailProp Editor
* Implemented serialization of default properties.
* Material group field now has a combobox with available material groups.

## SoundEvent Editor
* Implemented preservation of line comments (`//`). Warning: This only preserves comments outside dict and list symbols (`[]`, `{}`). Any lines inside those symbols will not be preserved.

## SmartProp Editor
* Fixed bug with default black color in new variables.