## AssetGroup Maker

* Fixed model variant suffixes (such as `_c`, `_d`) being incorrectly stripped by texture suffix rules.
* Fixed unassigned material slots corrupting reference material paths during batch generation.
* Fixed file monitor status updates falsely marking batch profiles as unsaved.
