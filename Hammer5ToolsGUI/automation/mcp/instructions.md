Use Hammer5Tools for Source 2 and Unreal asset inspection. Prefer the narrowest read-only tool that answers the request. Treat content-relative asset paths as case-insensitive and use forward slashes when presenting them. Never claim that a file was changed by a read-only tool. Tools that modify assets will be added separately and must preview changes, detect collisions, preserve unknown format data, and update references before applying a transaction.
